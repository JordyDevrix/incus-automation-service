instance:
  name: "${INSTANCE_VM_NAME}"
  os_image: "${INSTANCE_OS_IMAGE}"
  users:
    - username: "admin"
      ssh_keys:
        - "${INSTANCE_ADMIN_SSH_KEY}"

  resources:
    cpu: "${INSTANCE_CPU_COUNT}"
    ram: "${INSTANCE_RAM_SIZE}"
    disk: "${INSTANCE_DISK_SIZE}"